{
  "name": "resume",
  "version": "1.0.0",
  "description": "JSON Bulk Resume Builder, using JSON Resume, the open-source initiative to create a JSON-based standard for resumes.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build-resumes.js",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jsonresume-theme-even": "^0.26.1",
    "jsonresume-theme-paper": "^0.5.0",
    "jsonresume-theme-rickosborne": "^2022.9.16-T1450",
    "jsonresume-theme-rocketspacer": "^1.2.2",
    "jsonresume-theme-stackoverflow": "^2.1.0",
    "puppeteer": "^24.26.1",
    "resumed": "^6.1.0",
    "eslint": "^8.50.0"
  }
}
